I want to add spmd_types type checking to local_map regions (torch.distributed.tensor.experimental.local_map).
We add a kwarg to local_map(spmd_types: bool = False), that when True, performs local SPMD type checking inside the local_map region.
The API is still meant to work with plain tensors.

We need to assign local spmd types to the inputs, inferred from the DTensor in_placements of local_map.
One problem is that it is not known if a DTensor Replicate placement axis, corresponds to Replicate or Invariant in the local spmd_types system.
We can infer this from the existing grad argument in local_map (grad_in_placements), and the tied FWD/BWD typing of spmd_types (read the DESIGN md files).
If grad_in_placements is not specified, we default to Replicate.

Investigate what changes are needed.
Let's make a plan and write out an exemplar test. Make sure we use FakeProcessGroup/LocalTensor so the test is single process.
