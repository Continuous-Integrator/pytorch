diff --git a/torch/_dynamo/trace_rules.py b/torch/_dynamo/trace_rules.py
index 17cd668bb8e..25264979b64 100644
--- a/torch/_dynamo/trace_rules.py
+++ b/torch/_dynamo/trace_rules.py
@@ -175,6 +175,7 @@ manual_torch_name_rule_map: dict[
     "torch.distributed.distributed_c10d._get_group_tag": TorchInGraphFunctionVariable,
     "torch.distributed.distributed_c10d.get_process_group_ranks": TorchInGraphFunctionVariable,
     "torch._utils.is_compiling": TorchInGraphFunctionVariable,
+    "torch._utils._chunk_or_narrow_cat": UserFunctionVariable,
     "torch.fx._symbolic_trace.is_fx_tracing": TorchInGraphFunctionVariable,
     "torch.fx._symbolic_trace.is_fx_symbolic_tracing": TorchInGraphFunctionVariable,
     "torch._dynamo.external_utils.is_compiling": TorchInGraphFunctionVariable,
diff --git a/torch/_inductor/fx_passes/micro_pipeline_tp.py b/torch/_inductor/fx_passes/micro_pipeline_tp.py
index 6cc5503d481..afaa7ae426c 100644
--- a/torch/_inductor/fx_passes/micro_pipeline_tp.py
+++ b/torch/_inductor/fx_passes/micro_pipeline_tp.py
@@ -123,11 +123,35 @@ def find_all_gather_patterns(graph: torch.fx.Graph):
             KeywordArg("gather_dim"),
         )
 
-    # Matches funcol.all_gather_tensor with gather_dim > 0
+    # Matches funcol.all_gather_tensor with gather_dim > 0 (split+getitem path)
     non_zero_dim_all_gather_pattern = make_cat_pattern(
         make_all_gather_split_pattern(KeywordArg("shard")),
     )
 
+    # Matches funcol.all_gather_tensor with gather_dim > 0 (slice path,
+    # generated by _chunk_or_narrow_cat during tracing)
+    def make_all_gather_slice_pattern(shard):
+        return CallFunction(
+            aten.slice.Tensor,
+            CallFunction(
+                c10d.wait_tensor.default,
+                CallFunction(
+                    c10d.all_gather_into_tensor.default,
+                    shard,
+                    Ignored(),
+                    KeywordArg("group_name"),
+                ),
+                _users=MULTIPLE,
+            ),
+            Ignored(),
+            Ignored(),
+            Ignored(),
+        )
+
+    slice_based_non_zero_dim_all_gather_pattern = make_cat_pattern(
+        make_all_gather_slice_pattern(KeywordArg("shard")),
+    )
+
     # Match a zero-dim all-gather in which the data is transferred as uint8 and
     # viewed back as the original dtype.
     zero_dim_type_erased_all_gather_pattern = CallFunction(
@@ -160,14 +184,15 @@ def find_all_gather_patterns(graph: torch.fx.Graph):
     # should appear before (2) in the list.
     res_node_target_to_patterns = {
         aten.cat.default: [
-            (non_zero_dim_all_gather_pattern, 0),
+            non_zero_dim_all_gather_pattern,
+            slice_based_non_zero_dim_all_gather_pattern,
         ],
         aten.view.dtype: [
-            (non_zero_dim_type_erased_all_gather_pattern, 0),
-            (zero_dim_type_erased_all_gather_pattern, 0),
+            non_zero_dim_type_erased_all_gather_pattern,
+            zero_dim_type_erased_all_gather_pattern,
         ],
         c10d.wait_tensor.default: [
-            (zero_dim_all_gather_pattern, 0),
+            zero_dim_all_gather_pattern,
         ],
     }
 
@@ -175,17 +200,20 @@ def find_all_gather_patterns(graph: torch.fx.Graph):
     all_gathers = []
     visited_ag_nodes = OrderedSet[torch.fx.Node]()
     for node in reversed(graph.nodes):
-        for target, patterns in res_node_target_to_patterns.items():
+        for target, target_patterns in res_node_target_to_patterns.items():
             if node.target != target:
                 continue
-            for pattern, ag_node_idx in patterns:
+            for pattern in target_patterns:
                 match = pattern.match(node)
                 if not match:
                     continue
 
                 assert isinstance(match, Match)
-                ag_node = match.nodes[ag_node_idx]
-                assert ag_node.target == c10d.all_gather_into_tensor.default
+                ag_node = next(
+                    n
+                    for n in match.nodes
+                    if n.target == c10d.all_gather_into_tensor.default
+                )
 
                 if ag_node in visited_ag_nodes:
                     continue
@@ -314,6 +342,30 @@ def find_reduce_scatter_patterns(graph: torch.fx.Graph):
         users=2,
     )
 
+    # Matches funcol.reduce_scatter_tensor with scatter_dim > 0 (slice path,
+    # generated by _chunk_or_narrow_cat during tracing)
+    def make_slice_rearrange_pattern():
+        return CallFunction(
+            aten.cat.default,
+            ListOf(
+                CallFunction(
+                    aten.slice.Tensor,
+                    KeywordArg("input"),
+                    KeywordArg("scatter_dim"),
+                    Ignored(),
+                    Ignored(),
+                ),
+            ),
+        )
+
+    slice_based_non_zero_dim_reduce_scatter_pattern_single_user = (
+        reduce_scatter_template(make_slice_rearrange_pattern(), users=1)
+    )
+
+    slice_based_non_zero_dim_reduce_scatter_pattern_multi_user = (
+        reduce_scatter_template(make_slice_rearrange_pattern(), users=2)
+    )
+
     reduce_scatters = []
     for node in reversed(graph.nodes):
         if node.target == c10d.wait_tensor.default:
@@ -330,6 +382,21 @@ def find_reduce_scatter_patterns(graph: torch.fx.Graph):
                         group_name=match.kwargs["group_name"],
                     )
                 )
+            elif match := slice_based_non_zero_dim_reduce_scatter_pattern_single_user.match(
+                node
+            ):
+                assert isinstance(match, Match)
+                reduce_scatters.append(
+                    _ReduceScatterMatch(
+                        match=match,
+                        input_node=match.kwargs["input"],
+                        reduce_scatter_node=match.nodes[-2],
+                        wait_tensor_node=node,
+                        reduce_op=match.kwargs["reduce_op"],
+                        scatter_dim=match.kwargs["scatter_dim"],
+                        group_name=match.kwargs["group_name"],
+                    )
+                )
             elif match := zero_dim_reduce_scatter_pattern_single_user.match(node):
                 assert isinstance(match, Match)
                 reduce_scatters.append(
@@ -356,6 +423,21 @@ def find_reduce_scatter_patterns(graph: torch.fx.Graph):
                         group_name=match.kwargs["group_name"],
                     )
                 )
+            elif match := slice_based_non_zero_dim_reduce_scatter_pattern_multi_user.match(
+                node
+            ):
+                assert isinstance(match, Match)
+                reduce_scatters.append(
+                    _ReduceScatterMatch(
+                        match=match,
+                        input_node=match.kwargs["input"],
+                        reduce_scatter_node=match.nodes[-2],
+                        wait_tensor_node=node,
+                        reduce_op=match.kwargs["reduce_op"],
+                        scatter_dim=match.kwargs["scatter_dim"],
+                        group_name=match.kwargs["group_name"],
+                    )
+                )
             elif match := zero_dim_reduce_scatter_pattern_multi_user.match(node):
                 assert isinstance(match, Match)
                 reduce_scatters.append(
@@ -912,10 +994,12 @@ def fuse_matmul_reduce_scatter(reduce_scatter: _ReduceScatterMatch) -> None:
         filter_matmul = _filter_out_scaled_matmul
 
     # Currently fused_matmul_reduce_scatter doesn't return the matmul result,
-    # so we can't apply the fusion if the matmul result is used by multiple
-    # users. This is not a fundamental limitation of the fused op and can be
-    # addressed if needed.
-    if len(input_node.users) != 1:
+    # so we can't apply the fusion if the matmul result is used outside the
+    # reduce scatter pattern. For scatter_dim > 0, the input_node may have
+    # multiple users (the slice/split nodes), but they are all part of the
+    # reduce scatter pattern and will be erased.
+    match_nodes = set(reduce_scatter.match.nodes)
+    if any(u not in match_nodes for u in input_node.users):
         log.warning(
             "matmul result has more than one user, skipping fused_matmul_reduce_scatter fusion."
         )
