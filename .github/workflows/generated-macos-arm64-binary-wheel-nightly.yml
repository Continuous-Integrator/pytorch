# @generated DO NOT EDIT MANUALLY

# Template is at:    .github/templates/macos_binary_build_workflow.yml.j2
# Generation script: .github/scripts/generate_ci_workflows.py
name: macos-arm64-binary-wheel

on:
# TODO: Migrate to new ciflow trigger, reference https://github.com/pytorch/pytorch/pull/70321
  push:
    # NOTE: Meta Employees can trigger new nightlies using: https://fburl.com/trigger_pytorch_nightly_build
    branches:
      - nightly
    tags:
      # NOTE: Binary build pipelines should only get triggered on release candidate builds
      # Release candidate tags look like: v1.11.0-rc1
      - v[0-9]+.[0-9]+.[0-9]+-rc[0-9]+
      - 'ciflow/binaries/*'
      - 'ciflow/binaries_libtorch/*'
      - 'ciflow/binaries_wheel/*'
  workflow_dispatch:

env:
  ALPINE_IMAGE: "308535385114.dkr.ecr.us-east-1.amazonaws.com/tool/alpine"
  AWS_DEFAULT_REGION: us-east-1
  BUILD_ENVIRONMENT: macos-arm64-binary-wheel
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  PR_NUMBER: ${{ github.event.pull_request.number }}
  SKIP_ALL_TESTS: 0
concurrency:
  group: macos-arm64-binary-wheel-${{ github.event.pull_request.number || github.ref_name }}-${{ github.ref_type == 'branch' && github.sha }}-${{ github.event_name == 'workflow_dispatch' }}
  cancel-in-progress: true

jobs:
  # Builds every Python version sequentially on a single runner. libtorch_cpu
  # is Python-ABI-free and shared across iterations via `build/` — only
  # libtorch_python + _C are recompiled per interpreter (~5 min each after the
  # first ~80 min full build), thanks to the Python-cache invalidation in
  # tools/setup_helpers/cmake.py.
  wheel-build:
    if: ${{ github.repository_owner == 'pytorch' }}
    runs-on: macos-26-xlarge
    timeout-minutes: 240
    env:
      PYTORCH_ROOT: ${{ github.workspace }}
      PACKAGE_TYPE: wheel
      # TODO: This is a legacy variable that we eventually want to get rid of in
      #       favor of GPU_ARCH_VERSION
      DESIRED_CUDA: cpu
      GPU_ARCH_TYPE: cpu
      SKIP_ALL_TESTS: 1
      PYTORCH_EXTRA_INSTALL_REQUIREMENTS: cuda-toolkit[nvrtc,cudart,cupti,cufft,curand,cusolver,cusparse,cufile,nvjitlink,nvtx]==13.0.2; platform_system == 'Linux' | nvidia-cublas>=13.1.0.3,<=13.1.1.3; platform_system == 'Linux' | cuda-bindings>=13.0.3,<14; platform_system == 'Linux' | nvidia-cudnn-cu13==9.20.0.48; platform_system == 'Linux' | nvidia-cusparselt-cu13==0.8.1; platform_system == 'Linux' | nvidia-nccl-cu13==2.29.7; platform_system == 'Linux' | nvidia-nvshmem-cu13==3.4.5; platform_system == 'Linux'
    steps:
      # NOTE: These environment variables are put here so that they can be applied on every job equally
      #       They are also here because setting them at a workflow level doesn't give us access to the
      #       runner.temp variable, which we need.
      - name: Populate runner env
        shell: bash
        run: |
          # shellcheck disable=SC2129
          echo "BINARY_ENV_FILE=${RUNNER_TEMP}/env" >> "${GITHUB_ENV}"
      - name: Checkout PyTorch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
          submodules: recursive
          show-progress: false
      - name: Clean PyTorch checkout
        run: |
          # Remove any artifacts from the previous checkouts
          git clean -fxd
      - name: Install libomp
        run: "${PYTORCH_ROOT}/.ci/wheel/install_libomp.sh"
      # Build phase: compile a wheel per CPython version without delocating.
      # libtorch_cpu is reused across iterations via `build/`; only libtorch_python
      # + _C recompile thanks to the Python-cache invalidation in cmake.py.
      - name: Setup Python 3.10
        uses: actions/setup-python@v6
        with:
          # .4 version is min minor for 3.10, and also no-gil version of 3.13 needs at least 3.13.3
          python-version: "3.10.4"
          freethreaded: false
      - name: Build wheel 3.10
        env:
          DESIRED_PYTHON: "3.10"
          # Per-iteration output/work dirs so wheels and whl_tmp_dir stay isolated.
          PYTORCH_FINAL_PACKAGE_DIR: ${{ runner.temp }}/artifacts/wheel-py3_10-cpu
          MAC_PACKAGE_WORK_DIR: ${{ runner.temp }}/work/wheel-py3_10-cpu
        run: |
          set -eux -o pipefail
          mkdir -p "$PYTORCH_FINAL_PACKAGE_DIR" "$MAC_PACKAGE_WORK_DIR"
          # Rewrite BINARY_ENV_FILE with values derived from this iteration's DESIRED_PYTHON.
          "${PYTORCH_ROOT}/.ci/pytorch/binary_populate_env.sh"
          # shellcheck disable=SC1090
          source "$BINARY_ENV_FILE"

          USE_PYTORCH_METAL_EXPORT=1
          USE_COREML_DELEGATE=1
          TORCH_PACKAGE_NAME="${TORCH_PACKAGE_NAME//-/_}"
          export USE_PYTORCH_METAL_EXPORT USE_COREML_DELEGATE TORCH_PACKAGE_NAME
          "${PYTORCH_ROOT}/.ci/wheel/build_wheel.sh"
      - name: Setup Python 3.11
        uses: actions/setup-python@v6
        with:
          # .4 version is min minor for 3.10, and also no-gil version of 3.13 needs at least 3.13.3
          python-version: "3.11.4"
          freethreaded: false
      - name: Build wheel 3.11
        env:
          DESIRED_PYTHON: "3.11"
          # Per-iteration output/work dirs so wheels and whl_tmp_dir stay isolated.
          PYTORCH_FINAL_PACKAGE_DIR: ${{ runner.temp }}/artifacts/wheel-py3_11-cpu
          MAC_PACKAGE_WORK_DIR: ${{ runner.temp }}/work/wheel-py3_11-cpu
        run: |
          set -eux -o pipefail
          mkdir -p "$PYTORCH_FINAL_PACKAGE_DIR" "$MAC_PACKAGE_WORK_DIR"
          # Rewrite BINARY_ENV_FILE with values derived from this iteration's DESIRED_PYTHON.
          "${PYTORCH_ROOT}/.ci/pytorch/binary_populate_env.sh"
          # shellcheck disable=SC1090
          source "$BINARY_ENV_FILE"

          USE_PYTORCH_METAL_EXPORT=1
          USE_COREML_DELEGATE=1
          TORCH_PACKAGE_NAME="${TORCH_PACKAGE_NAME//-/_}"
          export USE_PYTORCH_METAL_EXPORT USE_COREML_DELEGATE TORCH_PACKAGE_NAME
          "${PYTORCH_ROOT}/.ci/wheel/build_wheel.sh"
      - name: Setup Python 3.12
        uses: actions/setup-python@v6
        with:
          # .4 version is min minor for 3.10, and also no-gil version of 3.13 needs at least 3.13.3
          python-version: "3.12.4"
          freethreaded: false
      - name: Build wheel 3.12
        env:
          DESIRED_PYTHON: "3.12"
          # Per-iteration output/work dirs so wheels and whl_tmp_dir stay isolated.
          PYTORCH_FINAL_PACKAGE_DIR: ${{ runner.temp }}/artifacts/wheel-py3_12-cpu
          MAC_PACKAGE_WORK_DIR: ${{ runner.temp }}/work/wheel-py3_12-cpu
        run: |
          set -eux -o pipefail
          mkdir -p "$PYTORCH_FINAL_PACKAGE_DIR" "$MAC_PACKAGE_WORK_DIR"
          # Rewrite BINARY_ENV_FILE with values derived from this iteration's DESIRED_PYTHON.
          "${PYTORCH_ROOT}/.ci/pytorch/binary_populate_env.sh"
          # shellcheck disable=SC1090
          source "$BINARY_ENV_FILE"

          USE_PYTORCH_METAL_EXPORT=1
          USE_COREML_DELEGATE=1
          TORCH_PACKAGE_NAME="${TORCH_PACKAGE_NAME//-/_}"
          export USE_PYTORCH_METAL_EXPORT USE_COREML_DELEGATE TORCH_PACKAGE_NAME
          "${PYTORCH_ROOT}/.ci/wheel/build_wheel.sh"
      - name: Setup Python 3.13
        uses: actions/setup-python@v6
        with:
          # .4 version is min minor for 3.10, and also no-gil version of 3.13 needs at least 3.13.3
          python-version: "3.13.4"
          freethreaded: false
      - name: Build wheel 3.13
        env:
          DESIRED_PYTHON: "3.13"
          # Per-iteration output/work dirs so wheels and whl_tmp_dir stay isolated.
          PYTORCH_FINAL_PACKAGE_DIR: ${{ runner.temp }}/artifacts/wheel-py3_13-cpu
          MAC_PACKAGE_WORK_DIR: ${{ runner.temp }}/work/wheel-py3_13-cpu
        run: |
          set -eux -o pipefail
          mkdir -p "$PYTORCH_FINAL_PACKAGE_DIR" "$MAC_PACKAGE_WORK_DIR"
          # Rewrite BINARY_ENV_FILE with values derived from this iteration's DESIRED_PYTHON.
          "${PYTORCH_ROOT}/.ci/pytorch/binary_populate_env.sh"
          # shellcheck disable=SC1090
          source "$BINARY_ENV_FILE"

          USE_PYTORCH_METAL_EXPORT=1
          USE_COREML_DELEGATE=1
          TORCH_PACKAGE_NAME="${TORCH_PACKAGE_NAME//-/_}"
          export USE_PYTORCH_METAL_EXPORT USE_COREML_DELEGATE TORCH_PACKAGE_NAME
          "${PYTORCH_ROOT}/.ci/wheel/build_wheel.sh"
      - name: Setup Python 3.13t
        uses: actions/setup-python@v6
        with:
          # .4 version is min minor for 3.10, and also no-gil version of 3.13 needs at least 3.13.3
          python-version: "3.13.4"
          freethreaded: true
      - name: Build wheel 3.13t
        env:
          DESIRED_PYTHON: "3.13t"
          # Per-iteration output/work dirs so wheels and whl_tmp_dir stay isolated.
          PYTORCH_FINAL_PACKAGE_DIR: ${{ runner.temp }}/artifacts/wheel-py3_13t-cpu
          MAC_PACKAGE_WORK_DIR: ${{ runner.temp }}/work/wheel-py3_13t-cpu
        run: |
          set -eux -o pipefail
          mkdir -p "$PYTORCH_FINAL_PACKAGE_DIR" "$MAC_PACKAGE_WORK_DIR"
          # Rewrite BINARY_ENV_FILE with values derived from this iteration's DESIRED_PYTHON.
          "${PYTORCH_ROOT}/.ci/pytorch/binary_populate_env.sh"
          # shellcheck disable=SC1090
          source "$BINARY_ENV_FILE"

          USE_PYTORCH_METAL_EXPORT=1
          USE_COREML_DELEGATE=1
          TORCH_PACKAGE_NAME="${TORCH_PACKAGE_NAME//-/_}"
          export USE_PYTORCH_METAL_EXPORT USE_COREML_DELEGATE TORCH_PACKAGE_NAME
          "${PYTORCH_ROOT}/.ci/wheel/build_wheel.sh"
      - name: Setup Python 3.14
        uses: actions/setup-python@v6
        with:
          # .4 version is min minor for 3.10, and also no-gil version of 3.13 needs at least 3.13.3
          python-version: "3.14.0"
          freethreaded: false
      - name: Build wheel 3.14
        env:
          DESIRED_PYTHON: "3.14"
          # Per-iteration output/work dirs so wheels and whl_tmp_dir stay isolated.
          PYTORCH_FINAL_PACKAGE_DIR: ${{ runner.temp }}/artifacts/wheel-py3_14-cpu
          MAC_PACKAGE_WORK_DIR: ${{ runner.temp }}/work/wheel-py3_14-cpu
        run: |
          set -eux -o pipefail
          mkdir -p "$PYTORCH_FINAL_PACKAGE_DIR" "$MAC_PACKAGE_WORK_DIR"
          # Rewrite BINARY_ENV_FILE with values derived from this iteration's DESIRED_PYTHON.
          "${PYTORCH_ROOT}/.ci/pytorch/binary_populate_env.sh"
          # shellcheck disable=SC1090
          source "$BINARY_ENV_FILE"

          USE_PYTORCH_METAL_EXPORT=1
          USE_COREML_DELEGATE=1
          TORCH_PACKAGE_NAME="${TORCH_PACKAGE_NAME//-/_}"
          export USE_PYTORCH_METAL_EXPORT USE_COREML_DELEGATE TORCH_PACKAGE_NAME
          "${PYTORCH_ROOT}/.ci/wheel/build_wheel.sh"
      - name: Setup Python 3.14t
        uses: actions/setup-python@v6
        with:
          # .4 version is min minor for 3.10, and also no-gil version of 3.13 needs at least 3.13.3
          python-version: "3.14.0"
          freethreaded: true
      - name: Build wheel 3.14t
        env:
          DESIRED_PYTHON: "3.14t"
          # Per-iteration output/work dirs so wheels and whl_tmp_dir stay isolated.
          PYTORCH_FINAL_PACKAGE_DIR: ${{ runner.temp }}/artifacts/wheel-py3_14t-cpu
          MAC_PACKAGE_WORK_DIR: ${{ runner.temp }}/work/wheel-py3_14t-cpu
        run: |
          set -eux -o pipefail
          mkdir -p "$PYTORCH_FINAL_PACKAGE_DIR" "$MAC_PACKAGE_WORK_DIR"
          # Rewrite BINARY_ENV_FILE with values derived from this iteration's DESIRED_PYTHON.
          "${PYTORCH_ROOT}/.ci/pytorch/binary_populate_env.sh"
          # shellcheck disable=SC1090
          source "$BINARY_ENV_FILE"

          USE_PYTORCH_METAL_EXPORT=1
          USE_COREML_DELEGATE=1
          TORCH_PACKAGE_NAME="${TORCH_PACKAGE_NAME//-/_}"
          export USE_PYTORCH_METAL_EXPORT USE_COREML_DELEGATE TORCH_PACKAGE_NAME
          "${PYTORCH_ROOT}/.ci/wheel/build_wheel.sh"
      - name: Delocate all wheels
        run: "${PYTORCH_ROOT}/.ci/wheel/delocate_wheels.sh"
      # Smoke-test + upload phase: each iteration re-activates its Python
      # (cache hit from the build phase) and exercises the delocated wheel.
      - name: Setup Python 3.10 (smoke)
        uses: actions/setup-python@v6
        with:
          python-version: "3.10.4"
          freethreaded: false
      - name: Smoke-test wheel 3.10
        env:
          PYTORCH_FINAL_PACKAGE_DIR: ${{ runner.temp }}/artifacts/wheel-py3_10-cpu
        run: |
          set -eux -o pipefail
          venv_dir="${RUNNER_TEMP}/test_venv_wheel-py3_10-cpu"
          rm -rf "$venv_dir"
          python -mvenv "$venv_dir"
          # shellcheck disable=SC1091
          source "$venv_dir/bin/activate"
          pip install "$PYTORCH_FINAL_PACKAGE_DIR"/*.whl numpy -v
          python "${PYTORCH_ROOT}/.ci/pytorch/smoke_test/smoke_test.py" --package torchonly
          deactivate
      - name: Upload wheel-py3_10-cpu
        uses: actions/upload-artifact@v4.4.0
        if: always()
        with:
          name: wheel-py3_10-cpu
          retention-days: 14
          if-no-files-found: error
          path: ${{ runner.temp }}/artifacts/wheel-py3_10-cpu
      - name: Setup Python 3.11 (smoke)
        uses: actions/setup-python@v6
        with:
          python-version: "3.11.4"
          freethreaded: false
      - name: Smoke-test wheel 3.11
        env:
          PYTORCH_FINAL_PACKAGE_DIR: ${{ runner.temp }}/artifacts/wheel-py3_11-cpu
        run: |
          set -eux -o pipefail
          venv_dir="${RUNNER_TEMP}/test_venv_wheel-py3_11-cpu"
          rm -rf "$venv_dir"
          python -mvenv "$venv_dir"
          # shellcheck disable=SC1091
          source "$venv_dir/bin/activate"
          pip install "$PYTORCH_FINAL_PACKAGE_DIR"/*.whl numpy -v
          python "${PYTORCH_ROOT}/.ci/pytorch/smoke_test/smoke_test.py" --package torchonly
          deactivate
      - name: Upload wheel-py3_11-cpu
        uses: actions/upload-artifact@v4.4.0
        if: always()
        with:
          name: wheel-py3_11-cpu
          retention-days: 14
          if-no-files-found: error
          path: ${{ runner.temp }}/artifacts/wheel-py3_11-cpu
      - name: Setup Python 3.12 (smoke)
        uses: actions/setup-python@v6
        with:
          python-version: "3.12.4"
          freethreaded: false
      - name: Smoke-test wheel 3.12
        env:
          PYTORCH_FINAL_PACKAGE_DIR: ${{ runner.temp }}/artifacts/wheel-py3_12-cpu
        run: |
          set -eux -o pipefail
          venv_dir="${RUNNER_TEMP}/test_venv_wheel-py3_12-cpu"
          rm -rf "$venv_dir"
          python -mvenv "$venv_dir"
          # shellcheck disable=SC1091
          source "$venv_dir/bin/activate"
          pip install "$PYTORCH_FINAL_PACKAGE_DIR"/*.whl numpy -v
          python "${PYTORCH_ROOT}/.ci/pytorch/smoke_test/smoke_test.py" --package torchonly
          deactivate
      - name: Upload wheel-py3_12-cpu
        uses: actions/upload-artifact@v4.4.0
        if: always()
        with:
          name: wheel-py3_12-cpu
          retention-days: 14
          if-no-files-found: error
          path: ${{ runner.temp }}/artifacts/wheel-py3_12-cpu
      - name: Setup Python 3.13 (smoke)
        uses: actions/setup-python@v6
        with:
          python-version: "3.13.4"
          freethreaded: false
      - name: Smoke-test wheel 3.13
        env:
          PYTORCH_FINAL_PACKAGE_DIR: ${{ runner.temp }}/artifacts/wheel-py3_13-cpu
        run: |
          set -eux -o pipefail
          venv_dir="${RUNNER_TEMP}/test_venv_wheel-py3_13-cpu"
          rm -rf "$venv_dir"
          python -mvenv "$venv_dir"
          # shellcheck disable=SC1091
          source "$venv_dir/bin/activate"
          pip install "$PYTORCH_FINAL_PACKAGE_DIR"/*.whl numpy -v
          python "${PYTORCH_ROOT}/.ci/pytorch/smoke_test/smoke_test.py" --package torchonly
          deactivate
      - name: Upload wheel-py3_13-cpu
        uses: actions/upload-artifact@v4.4.0
        if: always()
        with:
          name: wheel-py3_13-cpu
          retention-days: 14
          if-no-files-found: error
          path: ${{ runner.temp }}/artifacts/wheel-py3_13-cpu
      - name: Setup Python 3.13t (smoke)
        uses: actions/setup-python@v6
        with:
          python-version: "3.13.4"
          freethreaded: true
      - name: Smoke-test wheel 3.13t
        env:
          PYTORCH_FINAL_PACKAGE_DIR: ${{ runner.temp }}/artifacts/wheel-py3_13t-cpu
        run: |
          set -eux -o pipefail
          venv_dir="${RUNNER_TEMP}/test_venv_wheel-py3_13t-cpu"
          rm -rf "$venv_dir"
          python -mvenv "$venv_dir"
          # shellcheck disable=SC1091
          source "$venv_dir/bin/activate"
          pip install "$PYTORCH_FINAL_PACKAGE_DIR"/*.whl numpy -v
          python "${PYTORCH_ROOT}/.ci/pytorch/smoke_test/smoke_test.py" --package torchonly
          deactivate
      - name: Upload wheel-py3_13t-cpu
        uses: actions/upload-artifact@v4.4.0
        if: always()
        with:
          name: wheel-py3_13t-cpu
          retention-days: 14
          if-no-files-found: error
          path: ${{ runner.temp }}/artifacts/wheel-py3_13t-cpu
      - name: Setup Python 3.14 (smoke)
        uses: actions/setup-python@v6
        with:
          python-version: "3.14.0"
          freethreaded: false
      - name: Smoke-test wheel 3.14
        env:
          PYTORCH_FINAL_PACKAGE_DIR: ${{ runner.temp }}/artifacts/wheel-py3_14-cpu
        run: |
          set -eux -o pipefail
          venv_dir="${RUNNER_TEMP}/test_venv_wheel-py3_14-cpu"
          rm -rf "$venv_dir"
          python -mvenv "$venv_dir"
          # shellcheck disable=SC1091
          source "$venv_dir/bin/activate"
          pip install "$PYTORCH_FINAL_PACKAGE_DIR"/*.whl numpy -v
          python "${PYTORCH_ROOT}/.ci/pytorch/smoke_test/smoke_test.py" --package torchonly
          deactivate
      - name: Upload wheel-py3_14-cpu
        uses: actions/upload-artifact@v4.4.0
        if: always()
        with:
          name: wheel-py3_14-cpu
          retention-days: 14
          if-no-files-found: error
          path: ${{ runner.temp }}/artifacts/wheel-py3_14-cpu
      - name: Setup Python 3.14t (smoke)
        uses: actions/setup-python@v6
        with:
          python-version: "3.14.0"
          freethreaded: true
      - name: Smoke-test wheel 3.14t
        env:
          PYTORCH_FINAL_PACKAGE_DIR: ${{ runner.temp }}/artifacts/wheel-py3_14t-cpu
        run: |
          set -eux -o pipefail
          venv_dir="${RUNNER_TEMP}/test_venv_wheel-py3_14t-cpu"
          rm -rf "$venv_dir"
          python -mvenv "$venv_dir"
          # shellcheck disable=SC1091
          source "$venv_dir/bin/activate"
          pip install "$PYTORCH_FINAL_PACKAGE_DIR"/*.whl numpy -v
          python "${PYTORCH_ROOT}/.ci/pytorch/smoke_test/smoke_test.py" --package torchonly
          deactivate
      - name: Upload wheel-py3_14t-cpu
        uses: actions/upload-artifact@v4.4.0
        if: always()
        with:
          name: wheel-py3_14t-cpu
          retention-days: 14
          if-no-files-found: error
          path: ${{ runner.temp }}/artifacts/wheel-py3_14t-cpu

  wheel-upload:
    if: ${{ github.repository_owner == 'pytorch' }}
    permissions:
      id-token: write
      contents: read
    needs: wheel-build
    strategy:
      fail-fast: false
      matrix:
        include:
          - desired_python: "3.10"
            build_name: wheel-py3_10-cpu
          - desired_python: "3.11"
            build_name: wheel-py3_11-cpu
          - desired_python: "3.12"
            build_name: wheel-py3_12-cpu
          - desired_python: "3.13"
            build_name: wheel-py3_13-cpu
          - desired_python: "3.13t"
            build_name: wheel-py3_13t-cpu
          - desired_python: "3.14"
            build_name: wheel-py3_14-cpu
          - desired_python: "3.14t"
            build_name: wheel-py3_14t-cpu
    with:
      PYTORCH_ROOT: /pytorch
      PACKAGE_TYPE: wheel
      DESIRED_CUDA: cpu
      GPU_ARCH_TYPE: cpu
      DOCKER_IMAGE: manylinux2_28-builder
      DOCKER_IMAGE_TAG_PREFIX: cpu
      DESIRED_PYTHON: ${{ matrix.desired_python }}
      build_name: ${{ matrix.build_name }}
      use_s3: False
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}
      R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
      R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
      R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
    uses: ./.github/workflows/_binary-upload.yml
  libtorch-cpu-shared-with-deps-release-extract:
    if: ${{ github.repository_owner == 'pytorch' }}
    needs: wheel-build
    runs-on: macos-26-xlarge
    timeout-minutes: 60
    env:
      DESIRED_CUDA: cpu
      LIBTORCH_VARIANT: shared-with-deps
    steps:
      - name: Checkout PyTorch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
          sparse-checkout: .ci/libtorch/
          show-progress: false
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.10.4"
          freethreaded: false
      - uses: actions/download-artifact@v4.1.7
        name: Download Wheel Artifact
        with:
          name: wheel-py3_10-cpu
          path: "${{ runner.temp }}/wheel_artifact/"
      - name: Extract libtorch from wheel
        run: |
          set -eux
          mkdir -p "${{ runner.temp }}/libtorch_output"
          python3 .ci/libtorch/extract_libtorch_from_wheel.py \
            --wheel-dir "${{ runner.temp }}/wheel_artifact" \
            --output-dir "${{ runner.temp }}/libtorch_output" \
            --platform macos \
            --desired-cuda "$DESIRED_CUDA" \
            --libtorch-variant "$LIBTORCH_VARIANT"
      - uses: actions/upload-artifact@v4.4.0
        if: always()
        with:
          name: libtorch-cpu-shared-with-deps-release
          retention-days: 14
          if-no-files-found: error
          path: "${{ runner.temp }}/libtorch_output/"
  libtorch-cpu-shared-with-deps-release-upload:  # Uploading
    if: ${{ github.repository_owner == 'pytorch' }}
    permissions:
      id-token: write
      contents: read
    needs: libtorch-cpu-shared-with-deps-release-extract
    with:
      PYTORCH_ROOT: /pytorch
      PACKAGE_TYPE: libtorch
      DESIRED_CUDA: cpu
      GPU_ARCH_TYPE: cpu
      LIBTORCH_CONFIG: release
      LIBTORCH_VARIANT: shared-with-deps
      build_name: libtorch-cpu-shared-with-deps-release
      use_s3: False
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}
      R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
      R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
      R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
    uses: ./.github/workflows/_binary-upload.yml
